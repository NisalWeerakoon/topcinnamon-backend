# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/topcinnamon_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=1844

# JPA Properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# Character encoding
spring.datasource.sql-script-encoding=UTF-8
spring.jpa.properties.hibernate.connection.characterEncoding=utf-8
spring.jpa.properties.hibernate.connection.CharSet=utf-8
spring.jpa.properties.hibernate.connection.useUnicode=true

# Server Port
server.port=8081

# Logging
logging.level.com.topcinnamon=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG

# CORS
logging.level.org.springframework.web.cors=DEBUG

# Static Content
spring.web.resources.static-locations=classpath:/static/,classpath:/public/
spring.mvc.static-path-pattern=/**

# Error handling
server.error.include-message=always
server.error.include-stacktrace=never
server.error.include-binding-errors=always

# Add connection pool settings
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.maximum-pool-size=5

# ==================================
# Email Configuration (JavaMailSender)
# ==================================

# 1. Specify the SMTP server host
spring.mail.host=smtp.gmail.com 

# 2. Specify the port (e.g., 587 for TLS, 465 for SSL)
spring.mail.port=587 

# 3. Your email account username
# NOTE: For Gmail, use your email address.
spring.mail.username=pedhuruarachchigepremarathne@gmail.com

# 4. Your email password (use an App Password, NOT your main account password!)
# SECURITY WARNING: This password should be set via environment variable, NOT hardcoded.
spring.mail.password=Kkala@2006

# 5. Enable authentication and TLS/STARTTLS
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Admin Security (Basic setup - consider Spring Security for production)
admin.username=admin
admin.password=admin123

# CORS Configuration
app.cors.allowed-origins=http://localhost:3000,http://localhost:8080

# Pagination
spring.data.web.pageable.default-page-size=20
spring.data.web.pageable.max-page-size=100